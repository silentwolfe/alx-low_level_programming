#include <stdio.h>

/**
 * main - This prints out an output using printf function
 * Return: return 0 (success) else unsuccessful
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
