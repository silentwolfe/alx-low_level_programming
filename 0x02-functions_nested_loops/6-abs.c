#include "main.h"

/**
 * _abs - This function computes the absolute value of an integer
 * @i: integer prototype
 * Return: 0 success else failure
 */

int _abs(int i)
{
	if (i < 0)
	{
		return (-i);
	}
	else
	{
		return (i);
	}
}
