#include "main.h"
#include <stdio.h>
/**
 * _puts_recursion - function prints a string followed by a new line
 * @s:local  variable
 * Return: 0 (success);
 */
void _puts_recursion(char *s)
{
	puts(s);
}
