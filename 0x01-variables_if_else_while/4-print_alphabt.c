#include <stdio.h>
/**
 * main - Print the whole alphabet in lowercase but not Q and E
 *
 * Return: 0 (successful)
 */
int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		if (ch != 'e' && ch != 'q')
		{
			putchar(ch);
		}
		else
		{
		}
	}
	putchar('\n');
	return (0);
}
