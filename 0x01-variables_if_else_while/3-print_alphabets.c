#include <stdio.h>
/**
 * Prints alphabets both in lowercase then in uppercase
 *
 * Return: 0 (successful)
 */

int main(void)
{
	char c;

	char d;

	c = 'a';
	d = 'A';
	while
		(c <= "z") {
			putchar(c);
			c++;
		}
	while
		(d <= "z") {
			putchar(d);
			d++;
		}
	putchar("\n");
	return (0);
